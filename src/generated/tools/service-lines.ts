// Auto-generated by scripts/generate-tools.ts — DO NOT EDIT
import type { ToolDefinition, GenericApiClient } from '../types.js';
import type { CallToolResult } from '@modelcontextprotocol/sdk/types.js';

export const get_service_lines_by_service_line_number: ToolDefinition = {
  schema: {
    name: 'get_service_lines_by_service_line_number',
    description: 'Get service line — Required permission: Service plan, View. — [GET /public/v2/service-lines/{serviceLineNumber}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "serviceLineNumber": {
                      "type": "string",
                      "description": "Service line number"
                }
          },
          "required": [
                "serviceLineNumber"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['serviceLineNumber'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/public/v2/service-lines/{serviceLineNumber}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const delete_service_lines_by_service_line_number: ToolDefinition = {
  schema: {
    name: 'delete_service_lines_by_service_line_number',
    description: 'Deactivate a service line — Required permission: Service plan, Edit. — [DELETE /public/v2/service-lines/{serviceLineNumber}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "serviceLineNumber": {
                      "type": "string",
                      "description": "Service line number"
                },
                "reasonForCancellation": {
                      "type": "string",
                      "description": "Optional reason for cancelling this service line"
                },
                "endNow": {
                      "type": "boolean",
                      "description": "If service should end now, or on next bill day. Default is false. Default: false",
                      "default": false
                }
          },
          "required": [
                "serviceLineNumber"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['serviceLineNumber'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const queryParamNames = ['reasonForCancellation', 'endNow'];
    const queryParams: Record<string, unknown> = {};
    for (const name of queryParamNames) {
      if (args[name] !== undefined) queryParams[name] = args[name];
    }
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/public/v2/service-lines/{serviceLineNumber}',
      pathParams,
      queryParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_service_lines: ToolDefinition = {
  schema: {
    name: 'get_service_lines',
    description: 'Get all service lines — Required permission: Service plan, View. — [GET /public/v2/service-lines]',
    inputSchema: {
          "type": "object",
          "properties": {
                "addressReferenceId": {
                      "type": "string",
                      "description": "Filter by an Address Reference ID"
                },
                "searchString": {
                      "type": "string",
                      "description": "Filter by fuzzy match of nickname, or exact match on UT ID, UT nickname, UT serial number, or service line number"
                },
                "dataPoolId": {
                      "type": "string",
                      "description": "Filter for service lines on a given data pool"
                },
                "page": {
                      "type": "number",
                      "description": "The index of the page, starting at 0. Page size is 100. Default: 0",
                      "default": 0
                },
                "orderByCreatedDateDescending": {
                      "type": "boolean",
                      "description": "Sort the paginated results by created date. Default: true",
                      "default": true
                }
          }
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const queryParamNames = ['addressReferenceId', 'searchString', 'dataPoolId', 'page', 'orderByCreatedDateDescending'];
    const queryParams: Record<string, unknown> = {};
    for (const name of queryParamNames) {
      if (args[name] !== undefined) queryParams[name] = args[name];
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/public/v2/service-lines',
      queryParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_service_lines: ToolDefinition = {
  schema: {
    name: 'post_service_lines',
    description: 'Create service line — Required permission: Service plan, Edit.<br/>Service line must be linked to an address and a subscription/product-ID, and can optionally be immediately configured with data blocks. — [POST /public/v2/service-lines]',
    inputSchema: {
          "type": "object",
          "properties": {
                "addressReferenceId": {
                      "type": "string",
                      "description": "Address Reference ID to associate with the service line. Example: 55ec6574-10d8-bd9c-1951-d4184f4ae467"
                },
                "productReferenceId": {
                      "type": "string",
                      "description": "Subscription Product ID to associate with the service line. Example: business-subscription-100"
                },
                "dataBlockProducts": {
                      "type": "object",
                      "description": "dataBlockProducts"
                }
          },
          "required": [
                "addressReferenceId",
                "productReferenceId"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['addressReferenceId', 'productReferenceId', 'dataBlockProducts'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/public/v2/service-lines',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const put_service_lines_by_service_line_number_nickname: ToolDefinition = {
  schema: {
    name: 'put_service_lines_by_service_line_number_nickname',
    description: 'Set service line nickname — Required permission: Service plan, Edit. — [PUT /public/v2/service-lines/{serviceLineNumber}/nickname]',
    inputSchema: {
          "type": "object",
          "properties": {
                "serviceLineNumber": {
                      "type": "string",
                      "description": "Service line number"
                },
                "nickname": {
                      "type": "string",
                      "description": "New nickname for the existing service line."
                }
          },
          "required": [
                "serviceLineNumber",
                "nickname"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['serviceLineNumber'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['nickname'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/public/v2/service-lines/{serviceLineNumber}/nickname',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const put_service_lines_by_service_line_number_product: ToolDefinition = {
  schema: {
    name: 'put_service_lines_by_service_line_number_product',
    description: 'Update service line product — Required permission: Service plan, Edit. — [PUT /public/v2/service-lines/{serviceLineNumber}/product]',
    inputSchema: {
          "type": "object",
          "properties": {
                "serviceLineNumber": {
                      "type": "string",
                      "description": "Service line number"
                },
                "productReferenceId": {
                      "type": "string",
                      "description": "Product Id."
                },
                "recurringDataBlocks": {
                      "type": "array",
                      "description": "Recurring data blocks to configure."
                },
                "dataPoolId": {
                      "type": "string",
                      "description": "Pre-Release: If provided, onboard the service line onto an existing multi-service-line data pool. This parameter is mutually exclusive with RecurringDataBlocks. Available for select audiences only."
                },
                "delayUpdate": {
                      "type": "boolean",
                      "description": "Specifies when the change will be applied. True forces changes to be scheduled on the next billing cycle, False forces changes to occur immediately, Null uses legacy logic allowing upgrades to occur immediately and downgrades to be delayed."
                }
          },
          "required": [
                "serviceLineNumber",
                "productReferenceId"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['serviceLineNumber'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['productReferenceId', 'recurringDataBlocks', 'dataPoolId', 'delayUpdate'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/public/v2/service-lines/{serviceLineNumber}/product',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const put_service_lines_by_service_line_number_public_ip: ToolDefinition = {
  schema: {
    name: 'put_service_lines_by_service_line_number_public_ip',
    description: 'Set service line public IP setting — Required permission: Device command and configuration, Edit.<br/>Enable or disable public ip setting on a service line — [PUT /public/v2/service-lines/{serviceLineNumber}/public-ip]',
    inputSchema: {
          "type": "object",
          "properties": {
                "serviceLineNumber": {
                      "type": "string",
                      "description": "Service line number"
                },
                "publicIp": {
                      "type": "boolean",
                      "description": "If public IP should be enabled or not"
                }
          },
          "required": [
                "serviceLineNumber",
                "publicIp"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['serviceLineNumber'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['publicIp'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/public/v2/service-lines/{serviceLineNumber}/public-ip',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_service_lines_by_service_line_number_data_opt_in: ToolDefinition = {
  schema: {
    name: 'post_service_lines_by_service_line_number_data_opt_in',
    description: 'Opt in priority data — Required permission: Service plan, Edit.<br/>Opt in the service line to continue using priority data after reaching the plan capacity. Only applies to some products. — [POST /public/v2/service-lines/{serviceLineNumber}/data/opt-in]',
    inputSchema: {
          "type": "object",
          "properties": {
                "serviceLineNumber": {
                      "type": "string",
                      "description": "Service line number"
                }
          },
          "required": [
                "serviceLineNumber"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['serviceLineNumber'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/public/v2/service-lines/{serviceLineNumber}/data/opt-in',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_service_lines_by_service_line_number_data_opt_out: ToolDefinition = {
  schema: {
    name: 'post_service_lines_by_service_line_number_data_opt_out',
    description: 'Opt out priority data — Required permission: Service plan, Edit.<br/>Opt out the service line from continuing to use priority data even when plan capacity is exceeded. If the service line reaches the plan capacity, it will switch to using standard data. Only applies to some products. — [POST /public/v2/service-lines/{serviceLineNumber}/data/opt-out]',
    inputSchema: {
          "type": "object",
          "properties": {
                "serviceLineNumber": {
                      "type": "string",
                      "description": "Service line number"
                }
          },
          "required": [
                "serviceLineNumber"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['serviceLineNumber'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/public/v2/service-lines/{serviceLineNumber}/data/opt-out',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_service_lines_by_service_line_number_user_terminals: ToolDefinition = {
  schema: {
    name: 'post_service_lines_by_service_line_number_user_terminals',
    description: 'Add user terminal to service line — Required permission: Service plan, Edit.<br/>User terminal must already be on the account. See POST /v2/user-terminals to add user terminal to account. — [POST /public/v2/service-lines/{serviceLineNumber}/user-terminals]',
    inputSchema: {
          "type": "object",
          "properties": {
                "serviceLineNumber": {
                      "type": "string",
                      "description": "Service line number"
                },
                "deviceId": {
                      "type": "string",
                      "description": "User terminal Id, kit serial number, or dish serial number. Ex: 12345678-12345678-12345678"
                }
          },
          "required": [
                "serviceLineNumber",
                "deviceId"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['serviceLineNumber'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['deviceId'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/public/v2/service-lines/{serviceLineNumber}/user-terminals',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const delete_service_lines_by_service_line_number_user_terminals_by_device_id: ToolDefinition = {
  schema: {
    name: 'delete_service_lines_by_service_line_number_user_terminals_by_device_id',
    description: 'Remove user terminal from service line — Required permission: Service plan, Edit.<br/>This removes the user terminal from the service line, but it remains on the account and can be added to another service line. This clears any L2VPN circuits configured for the terminal. — [DELETE /public/v2/service-lines/{serviceLineNumber}/user-terminals/{deviceId}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "serviceLineNumber": {
                      "type": "string",
                      "description": "Service line number"
                },
                "deviceId": {
                      "type": "string",
                      "description": "User terminal ID, kit serial number, or dish serial number."
                }
          },
          "required": [
                "serviceLineNumber",
                "deviceId"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['serviceLineNumber', 'deviceId'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/public/v2/service-lines/{serviceLineNumber}/user-terminals/{deviceId}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const put_service_lines_by_service_line_number_data_recurring: ToolDefinition = {
  schema: {
    name: 'put_service_lines_by_service_line_number_data_recurring',
    description: 'Set recurring data blocks on service line — Required permission: Service plan, Edit.<br/>Service line must be on a top up plan — [PUT /public/v2/service-lines/{serviceLineNumber}/data/recurring]',
    inputSchema: {
          "type": "object",
          "properties": {
                "serviceLineNumber": {
                      "type": "string",
                      "description": "Service line number"
                },
                "recurringDataBlocks": {
                      "type": "array",
                      "description": "Recurring data blocks to configure"
                },
                "dataPoolId": {
                      "type": "string",
                      "description": "If provided, onboard the service line onto an existing data pool. The data pool must be multi-service-line enabled. This parameter is mutually exclusive with RecurringDataBlocks."
                },
                "applyToCurrentMonth": {
                      "type": "boolean",
                      "description": "When true, creates additional blocks for the current billing cycle so the current month data block counts match the new recurring configuration."
                }
          },
          "required": [
                "serviceLineNumber"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['serviceLineNumber'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['recurringDataBlocks', 'dataPoolId', 'applyToCurrentMonth'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/public/v2/service-lines/{serviceLineNumber}/data/recurring',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_service_lines_by_service_line_number_data_top_up: ToolDefinition = {
  schema: {
    name: 'post_service_lines_by_service_line_number_data_top_up',
    description: 'Adds one-time top up data block to service line — Required permission: Service plan, Edit.<br/>Service line must be on a top up plan — [POST /public/v2/service-lines/{serviceLineNumber}/data/top-up]',
    inputSchema: {
          "type": "object",
          "properties": {
                "serviceLineNumber": {
                      "type": "string",
                      "description": "The service line number"
                },
                "productId": {
                      "type": "string",
                      "description": "Product Id for data blocks"
                },
                "count": {
                      "type": "number",
                      "description": "Quantity of the specified data block to add"
                }
          },
          "required": [
                "serviceLineNumber",
                "productId",
                "count"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['serviceLineNumber'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['productId', 'count'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/public/v2/service-lines/{serviceLineNumber}/data/top-up',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_service_lines_by_service_line_number_billing_cycles_partial_periods: ToolDefinition = {
  schema: {
    name: 'get_service_lines_by_service_line_number_billing_cycles_partial_periods',
    description: 'Get billing partial periods — Required permission: Service plan, View.<br/>Get the previous billing partial periods for this service line. For more information about this endpoint, see https://starlink.readme.io/docs/understanding-proration. — [GET /public/v2/service-lines/{serviceLineNumber}/billing-cycles/partial-periods]',
    inputSchema: {
          "type": "object",
          "properties": {
                "serviceLineNumber": {
                      "type": "string",
                      "description": "Service line number"
                }
          },
          "required": [
                "serviceLineNumber"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['serviceLineNumber'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/public/v2/service-lines/{serviceLineNumber}/billing-cycles/partial-periods',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const patch_service_lines_by_service_line_number_consume_from_pool: ToolDefinition = {
  schema: {
    name: 'patch_service_lines_by_service_line_number_consume_from_pool',
    description: 'Pre-Release: Set whether a service line should consume from a data pool. — Required permission: Service plan, Edit.<br/>This endpoint is available for select audiences only. — [PATCH /public/v2/service-lines/{serviceLineNumber}/consume-from-pool]',
    inputSchema: {
          "type": "object",
          "properties": {
                "serviceLineNumber": {
                      "type": "string",
                      "description": "Service line number"
                },
                "consumeFromPool": {
                      "type": "boolean",
                      "description": "Whether the service line should consume from pool, defaults to true. Default: true",
                      "default": true
                }
          },
          "required": [
                "serviceLineNumber"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['serviceLineNumber'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const queryParamNames = ['consumeFromPool'];
    const queryParams: Record<string, unknown> = {};
    for (const name of queryParamNames) {
      if (args[name] !== undefined) queryParams[name] = args[name];
    }
    const response = await client.request({
      method: 'PATCH',
      pathTemplate: '/public/v2/service-lines/{serviceLineNumber}/consume-from-pool',
      pathParams,
      queryParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

// Auto-generated by scripts/generate-tools.ts — DO NOT EDIT
import type { ToolDefinition, GenericApiClient } from '../types.js';
import type { CallToolResult } from '@modelcontextprotocol/sdk/types.js';

export const get_contacts: ToolDefinition = {
  schema: {
    name: 'get_contacts',
    description: 'Get contacts — Required permission: User management, Edit.<br/>Get list of contacts on the authenticated user\\\'s account. — [GET /public/v2/contacts]',
    inputSchema: {
          "type": "object",
          "properties": {
                "page": {
                      "type": "number",
                      "description": "Page of contacts to query. Default: 0",
                      "default": 0
                }
          }
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const queryParamNames = ['page'];
    const queryParams: Record<string, unknown> = {};
    for (const name of queryParamNames) {
      if (args[name] !== undefined) queryParams[name] = args[name];
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/public/v2/contacts',
      queryParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_contacts: ToolDefinition = {
  schema: {
    name: 'post_contacts',
    description: 'Create contact — Required permission: Admin Only - API User Management, Edit.<br/>Creates the contact if it is new, and adds the contact to the authenticated account. — [POST /public/v2/contacts]',
    inputSchema: {
          "type": "object",
          "properties": {
                "firstName": {
                      "type": "string",
                      "description": "Contact first name."
                },
                "lastName": {
                      "type": "string",
                      "description": "Contact last name."
                },
                "roles": {
                      "type": "array",
                      "description": "Contact roles on account."
                },
                "email": {
                      "type": "string",
                      "description": "Contact email."
                },
                "phoneNumber": {
                      "type": "string",
                      "description": "Contact phone number."
                },
                "locale": {
                      "type": "string",
                      "description": "Contact locale."
                }
          },
          "required": [
                "firstName",
                "lastName",
                "roles",
                "email",
                "phoneNumber",
                "locale"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['firstName', 'lastName', 'roles', 'email', 'phoneNumber', 'locale'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/public/v2/contacts',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const put_contacts_by_subject_id: ToolDefinition = {
  schema: {
    name: 'put_contacts_by_subject_id',
    description: 'Update contact — Required permission: Admin Only - API User Management, Edit.<br/>Update contact on the authenticated account. — [PUT /public/v2/contacts/{subjectId}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "subjectId": {
                      "type": "string",
                      "description": "Subject Id of the contact to be updated."
                },
                "roles": {
                      "type": "array",
                      "description": "Contact roles on the account."
                },
                "phoneNumber": {
                      "type": "string",
                      "description": "Contact phone number."
                }
          },
          "required": [
                "subjectId"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['subjectId'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['roles', 'phoneNumber'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/public/v2/contacts/{subjectId}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const delete_contacts_by_subject_id: ToolDefinition = {
  schema: {
    name: 'delete_contacts_by_subject_id',
    description: 'Delete contact — Required permission: Admin Only - API User Management, Edit.<br/>Deletes contact based on specified subject Id on the authenticated account. — [DELETE /public/v2/contacts/{subjectId}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "subjectId": {
                      "type": "string",
                      "description": "Subject ID of contact to be deleted."
                }
          },
          "required": [
                "subjectId"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['subjectId'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/public/v2/contacts/{subjectId}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

// Auto-generated by scripts/generate-tools.ts — DO NOT EDIT
import type { ToolDefinition, GenericApiClient } from '../types.js';
import type { CallToolResult } from '@modelcontextprotocol/sdk/types.js';

export const get_routers_by_router_id: ToolDefinition = {
  schema: {
    name: 'get_routers_by_router_id',
    description: 'Get router — Required permission: Device management, View. — [GET /public/v2/routers/{routerId}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "routerId": {
                      "type": "string",
                      "description": "Router Id"
                }
          },
          "required": [
                "routerId"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['routerId'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/public/v2/routers/{routerId}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_routers_configs: ToolDefinition = {
  schema: {
    name: 'get_routers_configs',
    description: 'Get all router configs — Required permission: Device command and configuration, View.<br/>Return all router configs on this account in paginated form — [GET /public/v2/routers/configs]',
    inputSchema: {
          "type": "object",
          "properties": {
                "page": {
                      "type": "number",
                      "description": "Index of page to get. Page size is 100. Default: 0",
                      "default": 0
                }
          }
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const queryParamNames = ['page'];
    const queryParams: Record<string, unknown> = {};
    for (const name of queryParamNames) {
      if (args[name] !== undefined) queryParams[name] = args[name];
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/public/v2/routers/configs',
      queryParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_routers_configs: ToolDefinition = {
  schema: {
    name: 'post_routers_configs',
    description: 'Create router config — Required permission: Device command and configuration, Edit. — [POST /public/v2/routers/configs]',
    inputSchema: {
          "type": "object",
          "properties": {
                "nickname": {
                      "type": "string",
                      "description": "Nickname"
                },
                "routerConfigJson": {
                      "type": "string",
                      "description": "Router config json contents"
                }
          },
          "required": [
                "routerConfigJson"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['nickname', 'routerConfigJson'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/public/v2/routers/configs',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_routers_configs_by_config_id: ToolDefinition = {
  schema: {
    name: 'get_routers_configs_by_config_id',
    description: 'Get router config — Required permission: Device command and configuration, View. — [GET /public/v2/routers/configs/{configId}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "configId": {
                      "type": "string",
                      "description": "Config Id"
                }
          },
          "required": [
                "configId"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['configId'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/public/v2/routers/configs/{configId}',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const put_routers_configs_by_config_id: ToolDefinition = {
  schema: {
    name: 'put_routers_configs_by_config_id',
    description: 'Update router config — Required permission: Device command and configuration, Edit.<br/>Update a given router config. Any router assigned to this config will immediately receive the update if it is online. Otherwise, the router will receive the update when it comes online. — [PUT /public/v2/routers/configs/{configId}]',
    inputSchema: {
          "type": "object",
          "properties": {
                "configId": {
                      "type": "string",
                      "description": "Config Id"
                },
                "nickname": {
                      "type": "string",
                      "description": "Nickname"
                },
                "routerConfigJson": {
                      "type": "string",
                      "description": "Router config json contents"
                }
          },
          "required": [
                "configId",
                "routerConfigJson"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['configId'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const bodyParamNames = ['nickname', 'routerConfigJson'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/public/v2/routers/configs/{configId}',
      pathParams,
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const put_routers_configs_assign: ToolDefinition = {
  schema: {
    name: 'put_routers_configs_assign',
    description: 'Router config assignment — Required permission: Device configuration assignment, Edit.<br/>Assign the config (or none) to the routers. For each router if it is currently online, the config will immediately be sent to the router. Else, the config will be sent to the router when it comes online. Configs are sent to the router within 1-2 minutes. On error no assignment occurs. — [PUT /public/v2/routers/configs/assign]',
    inputSchema: {
          "type": "object",
          "properties": {
                "configId": {
                      "type": "string",
                      "description": "Config id (or none) to assign to routers"
                },
                "routerIds": {
                      "type": "array",
                      "description": "RouterIds to immediately update"
                }
          },
          "required": [
                "routerIds"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['configId', 'routerIds'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/public/v2/routers/configs/assign',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_routers_configs_default: ToolDefinition = {
  schema: {
    name: 'get_routers_configs_default',
    description: 'Get default router config — Required permission: Device command and configuration, View.<br/>Gets the router config id that will be assigned to any routers when they are first added to this account. — [GET /public/v2/routers/configs/default]',
    inputSchema: {
          "type": "object",
          "properties": {}
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/public/v2/routers/configs/default',
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const put_routers_configs_default: ToolDefinition = {
  schema: {
    name: 'put_routers_configs_default',
    description: 'Set default router config — Required permission: Device configuration assignment, Edit.<br/>Set the default router config on the account. Use an empty string to remove the default config from the account. Any NEW routers on this account will be assigned this config. — [PUT /public/v2/routers/configs/default]',
    inputSchema: {
          "type": "object",
          "properties": {
                "configId": {
                      "type": "string",
                      "description": "ConfigId or none to set as account default config. Use empty string to clear default config setting."
                }
          },
          "required": [
                "configId"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['configId'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/public/v2/routers/configs/default',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_routers_configs_tls: ToolDefinition = {
  schema: {
    name: 'get_routers_configs_tls',
    description: 'Get TLS configs — Required permission: Device command and configuration, View.<br/>Gets a paginated list of TLS configs on this account. — [GET /public/v2/routers/configs/tls]',
    inputSchema: {
          "type": "object",
          "properties": {
                "page": {
                      "type": "number",
                      "description": "Index of page to get. Page size is 100. Default: 0",
                      "default": 0
                }
          }
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const queryParamNames = ['page'];
    const queryParams: Record<string, unknown> = {};
    for (const name of queryParamNames) {
      if (args[name] !== undefined) queryParams[name] = args[name];
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/public/v2/routers/configs/tls',
      queryParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_routers_configs_tls: ToolDefinition = {
  schema: {
    name: 'post_routers_configs_tls',
    description: 'Create TLS config — Required permission: Device command and configuration, Edit.<br/>Creates a TLS config that allows this certificate and key to be used in router configs when a certificate and key are required by only specifying the certificate (matching key will be added). TLS configs are identified by their certificate string base64 encoded which must be unique on the account. — [POST /public/v2/routers/configs/tls]',
    inputSchema: {
          "type": "object",
          "properties": {
                "certificateBase64Pem": {
                      "type": "string",
                      "description": "TLS certificate in PEM format, base64 encoded."
                },
                "keyBase64Pem": {
                      "type": "string",
                      "description": "TLS key in PEM format, base64 encoded. Must match the public key of the certificate."
                }
          },
          "required": [
                "certificateBase64Pem",
                "keyBase64Pem"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['certificateBase64Pem', 'keyBase64Pem'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/public/v2/routers/configs/tls',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const delete_routers_configs_tls: ToolDefinition = {
  schema: {
    name: 'delete_routers_configs_tls',
    description: 'Delete TLS config — Required permission: Device command and configuration, Edit.<br/>Deletes a TLS certificate and key pair from the set of TLS configs that can be reused when saving router configs and only specifying the certificate. Does not affect any router configs that were already saved, just removes it from list of certificates that the matching key will be inserted on save for. — [DELETE /public/v2/routers/configs/tls]',
    inputSchema: {
          "type": "object",
          "properties": {
                "certificateBase64Pem": {
                      "type": "string",
                      "description": "Certificate base64 encoded pem string to match on for deletion of the corresponding certificate and key pair"
                }
          },
          "required": [
                "certificateBase64Pem"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['certificateBase64Pem'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'DELETE',
      pathTemplate: '/public/v2/routers/configs/tls',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_routers_local_content: ToolDefinition = {
  schema: {
    name: 'get_routers_local_content',
    description: 'Get list of router local content files — Required permission: Device command and configuration, View. — [GET /public/v2/routers/local-content]',
    inputSchema: {
          "type": "object",
          "properties": {}
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/public/v2/routers/local-content',
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_routers_local_content: ToolDefinition = {
  schema: {
    name: 'post_routers_local_content',
    description: 'Upload router local content file — Required permission: Device command and configuration, Edit.<br/>Upload html file to allow it to be configured as the HTTPS server local content file for router configs. File must be HTML, under 4MB, and file name under 100 characters. Files are stored in a public bucket for configured routers to download. File should be attached as multipart/form-data. See https://starlink.readme.io/docs/local-content for example upload script. — [POST /public/v2/routers/local-content]',
    inputSchema: {
          "type": "object",
          "properties": {
                "File": {
                      "type": "string",
                      "description": "File"
                }
          },
          "required": [
                "File"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['File'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/public/v2/routers/local-content',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const get_routers_sandbox_clients: ToolDefinition = {
  schema: {
    name: 'get_routers_sandbox_clients',
    description: 'Get sandbox clients — Required permission: Device command and configuration, View.<br/>Returns clients that were unsandboxed through the management API and have access that has not yet expired, paginated 1000 at a time. Clients that are allowed internet access due to sandbox disablement are not returned. — [GET /public/v2/routers/sandbox/clients]',
    inputSchema: {
          "type": "object",
          "properties": {
                "sandboxId": {
                      "type": "number",
                      "description": "Sandbox Id"
                },
                "expiryAfter": {
                      "type": "string",
                      "description": "Include clients whose access expires after this time"
                },
                "page": {
                      "type": "number",
                      "description": "Index of page to get. Page size is 1000. Default: 0",
                      "default": 0
                }
          }
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const queryParamNames = ['sandboxId', 'expiryAfter', 'page'];
    const queryParams: Record<string, unknown> = {};
    for (const name of queryParamNames) {
      if (args[name] !== undefined) queryParams[name] = args[name];
    }
    const response = await client.request({
      method: 'GET',
      pathTemplate: '/public/v2/routers/sandbox/clients',
      queryParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_routers_sandbox_clients: ToolDefinition = {
  schema: {
    name: 'post_routers_sandbox_clients',
    description: 'Batch update sandbox clients — Required permission: Device command and configuration, Edit.<br/>Update the sandbox state for multiple clients. If sandboxing is enabled, sandboxed clients will only have access to domains in the sandbox domain allow list. If a client is duplicated, records the client with the latest expiry. — [POST /public/v2/routers/sandbox/clients]',
    inputSchema: {
          "type": "object",
          "properties": {}
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const body: Record<string, unknown> = { ...args };
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/public/v2/routers/sandbox/clients',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const put_routers_sandbox_heartbeat: ToolDefinition = {
  schema: {
    name: 'put_routers_sandbox_heartbeat',
    description: 'Update sandbox heartbeat — Required permission: Device command and configuration, Edit.<br/>Heartbeats verify the health of enterprise systems that manage router sandboxing. If heartbeats are not received for an account, Starlink API will instruct routers under the account disable sandboxes until reboot. — [PUT /public/v2/routers/sandbox/heartbeat]',
    inputSchema: {
          "type": "object",
          "properties": {
                "healthy": {
                      "type": "boolean",
                      "description": "True if client systems are healthy, false otherwise."
                }
          },
          "required": [
                "healthy"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const bodyParamNames = ['healthy'];
    const body: Record<string, unknown> = {};
    for (const name of bodyParamNames) {
      if (args[name] !== undefined) body[name] = args[name];
    }
    const response = await client.request({
      method: 'PUT',
      pathTemplate: '/public/v2/routers/sandbox/heartbeat',
      body,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};

export const post_routers_by_router_id_reboot: ToolDefinition = {
  schema: {
    name: 'post_routers_by_router_id_reboot',
    description: 'Reboot router — Required permission: Device command and configuration, Edit. — [POST /public/v2/routers/{routerId}/reboot]',
    inputSchema: {
          "type": "object",
          "properties": {
                "routerId": {
                      "type": "string",
                      "description": "Router Id"
                }
          },
          "required": [
                "routerId"
          ]
    },
  },
  handler: async (args: Record<string, unknown>, client: GenericApiClient): Promise<CallToolResult> => {
    const pathParamNames = ['routerId'];
    const pathParams: Record<string, string> = {};
    for (const name of pathParamNames) {
      if (args[name] !== undefined) pathParams[name] = String(args[name]);
    }
    const response = await client.request({
      method: 'POST',
      pathTemplate: '/public/v2/routers/{routerId}/reboot',
      pathParams,
    });

    return {
      content: [{
        type: 'text' as const,
        text: JSON.stringify(response, null, 2),
      }],
    };
  },
};
